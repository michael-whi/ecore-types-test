{
  "name": "ecore-types",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "dependencies": {
    "ecore": "^0.12.0"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "devDependencies": {
    "typescript": "^3.5.3"
  },
  "scripts": {
    "build": "tsc ./src/index.ts --outDir ./dist/",
    "watch": "tsc -w -p .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
